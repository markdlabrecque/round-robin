# Cloudflare shared tracer bullet report

Goal: Land the shared Cloudflare Worker tracer bullet and stable contracts on the cloudflare-migration integration branch before parallel fan-out.

Acceptance criteria:
- The deployable app has one source tree under public/ while main remains unchanged.
- package.json and wrangler.jsonc provide local development, test, and dry-run deployment commands without live Cloudflare access.
- A Worker entrypoint serves static assets and a versioned JSON response at /api/health under wrangler dev.
- Existing browser import, scheduler, persistence, and static security behavior remain green after the layout move.
- An automated public-bundle allowlist rejects fixtures, raw email, secrets, and registrant data.
- Shared contracts define Worker bindings, D1 repository methods, roster/import JSON shapes, error codes, retention and input limits, and the email-ingestion boundary.
- Executable contract tests and the full existing project gate pass.

Approved plan path: /Users/mark/Projects/round-robin/docs/03-cloudflare-migration-implementation.md
Approved plan digest: 54b49508ed1e16c4dd7ddbaac0d879fdd876821d3419af0ad127306ab0139908

Implementation summary: Fixed the residual quoted and escaped-quoted serialized roster JSON bypass in the public-bundle guard and added regression coverage for allowlisted JS and HTML while preserving ordinary source references. The broader tracer bullet moved deployable assets to public/, added package and Wrangler setup, /api/health, public-bundle checks, and executable contracts.

Changed files: tests/cloudflare_tracer_bullet.test.js; package.json; package-lock.json; wrangler.jsonc; public; src; scripts/check_public_bundle.js; tests/deployed_site_contract.test.js; tests/registration_app_initialization.test.js; tests/registration_email_parser_behavior.test.js; tests/registration_import_state.test.js; tests/test_registration_email_parser.js; tests/public_bundle_allowlist.test.js; tests/contracts_semantics.test.js; tests/test_round_scheduler.js; src/contracts/roster.js; src/contracts/email_ingestion.js; src/repositories/roster_repository.js; public/demo_roster.json; round_scheduler.js.

Test evidence:
- Command: node --test tests/cloudflare_tracer_bullet.test.js. Result: failed as expected. Output: 2 failures: missing src/worker.js and public/index.html. Test pins wrangler.jsonc, src/contracts/, and src/repositories/roster_repository.js boundaries. Expected health JSON is { version: 1, status: 'ok' }. Failure reason: The repository does not yet contain the Worker composition root, /api/health behavior, public static asset tree, or shared contract boundaries required by the approved tracer bullet.
- Command: env -u PI_SUBAGENT_ID -u PI_SUBAGENT_ROLE -u PI_WORKFLOW_ID -u PI_WORKFLOW_ROLE npm test. Result: passed. Output: 21 tests passed; public-bundle allowlist passed. `npm exec --yes --package=wrangler@4.124.0 -- wrangler deploy --dry-run` passed. Local `wrangler dev` smoke passed for `/` and `/api/health`.
- Command: env -u PI_SUBAGENT_ID -u PI_SUBAGENT_ROLE -u PI_WORKFLOW_ID -u PI_WORKFLOW_ROLE npm test. Result: passed. Output: 21 tests passed; scripts/check_public_bundle.js completed successfully.
- Command: env -u PI_SUBAGENT_ID -u PI_SUBAGENT_ROLE -u PI_WORKFLOW_ID -u PI_WORKFLOW_ROLE npm test. Result: passed. Output: 25 tests passed; public-bundle allowlist passed. Targeted contract, bundle, scheduler, and tracer tests also passed.
- Command: env -u PI_SUBAGENT_ID -u PI_SUBAGENT_ROLE -u PI_WORKFLOW_ID -u PI_WORKFLOW_ROLE npm test. Result: passed. Output: 25 tests passed; public-bundle check passed.
- Command: env -u PI_SUBAGENT_ID -u PI_SUBAGENT_ROLE -u PI_WORKFLOW_ID -u PI_WORKFLOW_ROLE npm test. Result: passed. Output: 28 tests passed, 0 failures; targeted bundle and contract gate passed 7 tests.
- Command: env -u PI_SUBAGENT_ID -u PI_SUBAGENT_ROLE -u PI_WORKFLOW_ID -u PI_WORKFLOW_ROLE npm test. Result: passed. Output: 28 tests, 0 failures; public-bundle check passed.
- Command: env -u PI_SUBAGENT_ID -u PI_SUBAGENT_ROLE -u PI_WORKFLOW_ID -u PI_WORKFLOW_ROLE npm test. Result: passed. Output: Final independent verification approved: 28 tests, 0 failures; quoted and escaped roster JSON rejected; ordinary source references accepted; no unresolved findings.

Review verdict: not approved in the persisted workflow record because the two-round review cap was reached. Independent post-fix evidence passed, but the work could use more review passes.

Review findings:
- must_fix, Registrant payload detection only covers demo_roster.json. Detail: The content check permits registrant payloads embedded in other allowlisted text such as app.js. Add content-level detection and negative coverage across allowed text files.
- must_fix, Repository semantics remain mostly descriptive. Detail: Executable contracts do not validate listRecentRosters, findRosterById, deleteExpired, or saveRoster result shapes and argument behavior.
- must_fix, API envelope builders accept incomplete records. Detail: rosterEnvelope does not require createdAt and expiresAt, and importEnvelope does not require receivedAt. Add required-field and rejection tests.

Follow-ups: none.

Accepted deviation: code targeted_post_cap_fix. Reason: All three findings are concrete acceptance failures in the shared safety and compatibility contracts. They can be fixed narrowly in existing contract/check modules and tests without rescoping or accepting risk. Decision: accept_deviation. Risk: Post-cap findings remain: Registrant payload detection only covers demo_roster.json; Repository semantics remain mostly descriptive; API envelope builders accept incomplete records. Evidence: review-b788a4b71acc68a0a427835f9d32b1f4df6aebaa83ce20a01641bf18b2cc8f71.

Unresolved risk: Post-cap findings remain: Registrant payload detection only covers demo_roster.json; Repository semantics remain mostly descriptive; API envelope builders accept incomplete records.

No live Cloudflare resources were contacted. main was unchanged.
